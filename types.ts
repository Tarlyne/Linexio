import React from 'react';

// Global TypeScript types, interfaces, and enums.
// Import these in other files using: import { ... } from './types';

export interface BaseProps {
  className?: string;
  children?: React.ReactNode;
}